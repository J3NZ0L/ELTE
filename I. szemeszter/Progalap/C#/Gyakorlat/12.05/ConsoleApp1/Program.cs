namespace ConsoleApp1
{
    internal class Program
    {
        static void Main(string[] args)
        {
             //1. feladat

            /*
            Console.WriteLine("n: ");
            int n=int.Parse(Console.ReadLine());
            Console.WriteLine("Adatok: ");
            bool van = false;
            int maxi;
            int maxertek;
            List<int> adatok=new List<int>();
            for (int i = 0; i < n; i++)
            {
                adatok.Add(int.Parse(Console.ReadLine()));
            }
            Console.WriteLine("K: ");
            maxertek = 0;
            int K=int.Parse(Console.ReadLine());
            for (int i=0; i < n; i++)
            {
                if (adatok[i] >= K) { }
                else if (!van)
                {
                    van= true;
                    maxi = i;
                    maxertek = adatok[i];
                } else
                {
                    if (adatok[i] > maxertek) {
                        maxi = i;
                        maxertek = adatok[i];
                    }
                }
            }
            Console.WriteLine("Maximumertek ami K-nal kisebb: " + maxertek);
           */

            //2. feladat
        }
    }
}